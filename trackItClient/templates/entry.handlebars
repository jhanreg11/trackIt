<div class="entry">
    <div class="first">{{ entry.category }}</div>
    <div>{{ entry.item }}</div>
    <div>{{ entry.units }}</div>
    <div>{{ entry.amount }} ($)</div>
    <div>{{ entry.date.month }}/{{ entry.date.day }}/{{ entry.date.year }} </div>
</div>
